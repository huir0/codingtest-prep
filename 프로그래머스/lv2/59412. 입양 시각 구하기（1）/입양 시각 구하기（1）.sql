-- 코드를 입력하세요
SELECT CAST(DATE_FORMAT(DATETIME, '%H') AS UNSIGNED) AS HOUR, COUNT(ANIMAL_ID) FROM ANIMAL_OUTS WHERE DATE_FORMAT(DATETIME, '%H') > 8 AND DATE_FORMAT(DATETIME, '%H') < 20 GROUP BY DATE_FORMAT(DATETIME, '%H') ORDER BY DATE_FORMAT(DATETIME, '%H') ASC;